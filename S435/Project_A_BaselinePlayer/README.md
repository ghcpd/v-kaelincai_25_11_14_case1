# Project A - Baseline Player

Usage:
1. Setup env: `./setup.sh` (or `setup.ps1` on Windows)
2. Run tests: `./run_tests.sh`

Artifacts produced in `results/`, `logs/`, and screenshots in `screenshots/`.

Test scenarios are in `data/test_data.json`.
